# Configuration locale de l'IDE (WebStorm) : propre à chaque poste,
# la committer écraserait celle des autres membres de l'équipe
.idea/

# Dépendances Node (le projet n'en a aucune, mais npm en crée au besoin)
node_modules/

# Fichiers générés par le système
.DS_Store
Thumbs.db
